{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('test_vue_app') }}
{% endblock %}

{% block body %}
    <script charset="utf-8">
        window.user = {{ user|raw }};{% if album is defined %}
            window.album = {{ album|jsonld }};
        {% endif %}console.log(window.album);
    </script>
    <div id="app"></div>
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('test_vue_app') }}
{% endblock %}
