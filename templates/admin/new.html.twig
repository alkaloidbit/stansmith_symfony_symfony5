
{% extends 'admin_base.html.twig' %}

{% block body %}
<div class="text-center py-5 px-3">

    <h1>Create a MediaObject</h1>
    {{ form_start(mediaObjectForm) }}
        {{ form_row(mediaObjectForm.file) }}
        <button type="submit" class="btn btn-primary">Create</button>
        {{ form_end(mediaObjectForm) }}
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

{% endblock %}

